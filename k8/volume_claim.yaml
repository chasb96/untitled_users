apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: users-mongo-storage-volume-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
  volumeName: users-mongo-storage-volume