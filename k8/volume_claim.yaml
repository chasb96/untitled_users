apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: users-mongo-volume-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
  volumeName: users-mongo-volume